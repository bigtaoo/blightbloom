# UI, HUD & screen flow

The player-facing shell around the engine: the **HUD** during play, the **screens** that wrap a run (menu → loadout → in-match → result), and the **input surface** (twin-stick + corner buttons) that turns touches/keys into the engine's `PlayerCommand`. It builds on the all-Pixi UI decision (`00`), the `ui` render layer (`01`), the controls/orientation lock (`05`), the WeChat input+text constraints (`04`), and the engine↔render split (`06`/`08`). This doc is the source of truth for **what UI exists, how it reads engine state without touching it, and how input becomes a command**.

## The decisions (locked)

- **UI is drawn with Pixi, no DOM.** Every screen, HUD element, stick, and button is a Pixi `Container`/`Graphics`/`Text` in the `ui` layer (`00`, `01`). No HTML overlay — it would not exist on WeChat (`04`) and would split the render path. One engine, one renderer, one UI toolkit.
- **UI reads `state` + `events`, never mutates the engine.** The HUD renders from `GameState` (hp, weapon, enemy count, phase) and the per-frame `events` queue (`08`: `hit`, `deflect`, `death`, `pickup`) for transient feedback. It is downstream of the engine exactly like the render layer (`06`'s day-one split). The **only** channel UI→engine is a `PlayerCommand` submitted through the `InputSource` (`08`).
- **Input is quantized to a command at the UI edge.** Sticks/mouse/buttons are sampled each frame and converted to `moveBrad/moveMag` and a `buttons` bitfield **before** they reach the engine (`08`'s `PlayerCommand`, `06`'s "quantize at the input edge"). The engine never sees a screen pixel or a float angle. This is what keeps local play and net play on one code path.
- **Landscape-only, move stick + fire + corner buttons.** A single stick moves; a fire zone/left-click fires — there is no aim input at all (`10` v33): the engine auto-faces the nearest hostile, else the movement direction. Corner buttons are weapon-1 / weapon-2 plus a held INTERACT (`05`, `04`) — and since 2026-09-03 the two weapon buttons really do pick their own slot: each resolves to the engine's single `SWAP_WEAPON` toggle only when the slot it names is not already active (`controllers/weaponSlotSelect.ts`). Before that, `Game.onSwitchWeapon` discarded the `slot` argument both buttons pass and toggled unconditionally, so the two were indistinguishable and each label was wrong every other press. There is no jump or block button — parry is the melee swing. The layout assumes horizontal space and `deviceOrientation:"landscape"` (`05`). Portrait is dropped.
- **A screen state machine wraps the engine; the engine only knows `phase`.** `GameState.phase` is just `idle | playing | gameover` (`08`). Everything around a match — main menu, loadout/preset pick (`05`, `09`), pause, result/summary, restart — lives in a **render-side `ScreenManager`**, not in the engine. The engine is a pure match simulator; the shell decides which match to start and what to show before/after.

## Screen flow (shipped, 2026-08-03; the lobby + identity gate, 2026-09-10)

> **The run boundary is the one transition that is HELD (2026-09-22).** Crossing into a map or
> back out to the lobby shows `ui/loadingScreen.ts` for at least three seconds, whether or not
> anything is downloading — `controllers/TransitionGate.ts` owns it and `12` has the full account,
> including the two transitions deliberately left instant. Everything else in this section is as
> immediate as it reads: the hub screens below are ordinary navigation, and putting a floor on
> those would make the menu unusable. The BOOT splash is not held either, and the paragraph in
> `12` records why it briefly was.

The full loop a player can complete start-to-finish — closed the last structural gaps
(no menu-driven path into online play, no matchmaking feedback, no tutorial) in the
2026-08-03 pass below, and collapsed the two front-door screens that pass left behind into
one lobby on 2026-09-10; the diagram and every bullet in this section describe the
CURRENT shipped shape, not the original MVP sketch. **Two bullets below are marked
superseded rather than rewritten** — the shape they describe was real and shipped for five
weeks, and the reason it changed is the interesting part.

```
 Boot / splash          plain HTML/CSS spinner, removed once the identity gate below clears
   → Identity settle      the auto-login gate (2026-09-10): the spinner stays up until the
                          host's silent login has an ANSWER — an account or a guest — capped
                          by a budget so a hung SDK cannot hold the boot ✅ shipped
                          crazygames: `PortalAuth` · wechat: guest, no login exists there yet
                          · web: the stored session, read synchronously
   → Lobby                the front door AND the branch point, ONE screen ✅ shipped 2026-09-10
                          (was Main menu → Mode select, two screens, 2026-08-03 → 2026-09-10)
        ├─ Solo PvE       → Loadout (character + the kit you carry) → In-match
        ├─ Co-op          → Matchmaking (connecting/error, Cancel/Retry) → In-match
        ├─ PvP solo queue → PvP match preview (map/scaled build, QUEUE/Back) ✅ shipped
        │                  → Matchmaking (connecting/error, Cancel/Retry) → In-match
        ├─ Squad          pre-formed party (`PartyScreen`) → Matchmaking → In-match ✅ shipped
        │                 (no preview step here — see PvpPreview's own note below)
        ├─ Forge          the crafting page, its own screen since 2026-09-21 ✅ shipped
        │                 (also reachable from the Loadout screen's FORGE card; BACK
        │                  returns to whichever door it was opened from)
        ├─ Tutorial       → In-match directly (fixed loadout, no Loadout screen, skippable)
        └─ account chip   states who you are; opens `LoginScreen` only where a host allows a
                          login at all (design/20 forbids one on a portal) ✅ shipped
   → Loadout / preset     PvE: character + the weapons the run carries, and START RUN
                          (`screens/Loadout.ts`) ✅ shipped
                          PvP: only one preset/map exist today, so `PvpPreview` is a
                          confirm/preview step, not a picker (a 2nd preset is additive) ✅ shipped
   → Matchmaking          real connecting/error feedback, replacing a blank `playing`
                          phase that used to hang forever on a dropped connection ✅ shipped
   → In-match             HUD + controls; engine phase = playing
        ├─ Pause          overlay; engine keeps last state, no ticks advance
        └─ (net) waiting   "waiting for players / reconnecting" (06 stall)
   → Result               clear / death / (PvP) win-loss  → summary ✅ shipped
   → back to the lobby or "run again" (re-seed, rebuild state) ✅ shipped
```

- ✅ **The forge split in two, and the pre-run screen got a face (2026-09-21)** (`client/src/game/screens/Loadout.ts` is new; `screens/Forge.ts` kept only the crafting half) — reported against a screenshot of the screen SOLO PvE opens, with the character line circled: *"give the selected character a picture with the text beside it; show only the weapons already forged, with a jump to the forge at the end of the row; put forging on its own page with its own lobby entrance"*. **One screen was answering two questions.** "Who am I taking in, with what?" and "what should I spend my materials on?" shared a screen, and the second one took four fifths of the pixels: a paged grid of the whole `BLUEPRINT_CATALOG`, most of it locked or unaffordable, sat where the kit should have been. So the crafting grid stayed in `Forge.ts` as a page of its own and everything else moved to `Loadout.ts` — the character (portrait + name + pools, art read from the rig bundle's `shell` slot the same way `PlayerCard` already does, so a new character needs no extra art), the weapon row, and the action bar. Four things worth recording because each was a decision rather than a move:
  - **The weapon row is `resolveLoadout`'s answer, not `m.loadout`.** An empty loadout is not an empty kit — every run carries a gun AND a melee weapon (`ENGINE_VERSION` 45) — so a bare read of the staged list would show a player nothing at all right before handing them two weapons. The cards say which is which: forged carries the ▸ badge and a green `forged`, a filled-in starter carries neither and reads `default kit` in grey — and is deliberately NOT drawn with the locked styling the forge grid uses, because it is a weapon the run IS carrying. The row ends with a FORGE card, because "this is what I am carrying" is exactly the question that raises "how do I carry something better".
  - **BACK on the forge is a recorded door, not a fixed destination** (`RunState.forgeReturnPhase`, the shape `matchmakingReturnPhase` already had). The page has two entrances now, and a fixed BACK drops a player who was two clicks from starting a run back onto the front door.
  - **The lobby's FORGE row shares a line with SQUAD**, and that is arithmetic rather than taste: the tallest legal lobby — a portal build's quick-play row and data notice, the longest legal maintenance banner, and a resumable save — already measures 630 of a 640-px design height in all eight locales, so there were 10 px to spend and a full-width row costs 47. What makes a two-up row safe here where CO-OP/PVP QUEUE was not (see the bullet below) is the labels: 8 characters at worst (`SCHMIEDE`, `ESCOUADE`) against ~87 px, where `PVP SOLO QUEUE` needed 169. `labelFit.test.ts` checks it in every locale instead of trusting the sum.
  - **The overlap it shipped for one screenshot is a gate now** (`screens/widgetOverlap.test.ts`, new). `CLEAR LOADOUT` aligned to the weapon row's left edge (`cx - 212`) was drawn 58px underneath `START RUN` (`cx - 110`), and **both** existing sweeps stayed green — correctly: the button was inside the design space (`viewportFit`) and its label was inside its own box (`labelFit`). It was found by looking at a screenshot, which is not a gate. The new sweep is the third reading of the same sentence those two files already carry — *"nothing is off screen, never nothing collides"* — and it asks the remaining half: do two PRESS TARGETS share pixels? It measures each widget's background `Graphics` (child 0, the shape a press actually lands on, since the label is `eventMode: 'none'`), so it needs no text measurement and reads the same in every environment; it covers `BlueprintCard` as well as `Button`, because this screen's weapon row is cards and its action bar is buttons and they are laid out against each other. Restoring the old geometry turns **18 of its cases red** across the eight locales, which is how it was checked rather than by trusting that it would have.
  - **The keyboard table split with the screens.** `[1-9]`/`[↑↓]`/`[B]` act on the grid and run only in the forge phase; `[C]`/`[X]`/`[Enter]` act on the kit and run only in the loadout phase; `[F]` is the new bridge. A merged table would let `[X]` empty a loadout whose cards are not on screen. The floating SETTINGS button moved with the pre-run half for the same reason, so `SettingsReturnPhase` is `'menu' | 'loadout' | 'paused'` now.
- ✅ **The lobby: one front door instead of two (2026-09-10)** (`client/src/game/screens/MainMenu.ts` + the new `client/src/game/ui/LobbyRoutes.ts`; `ModeSelect.ts` deleted) — reported against a screenshot of the shipped menu with SQUAD and LOGIN circled: *"does this page need a redesign — then you go through into the lobby, and the lobby is where levels, matchmaking and squad belong"*. What the screenshot actually shows is one screen doing three jobs — title card, account entry, mode entry — and the incoherence that follows: **"play with other people" had two doors that were never adjacent.** SQUAD (a pre-formed PvP party) sat on the title screen as a peer of PLAY, while CO-OP and PVP SOLO QUEUE sat one screen deeper, behind it. Merging the two screens is not an extra layer, it removes one: four screens to a run (menu → modeSelect → forge → run) became three, and the five routes became siblings. **This is a content change, not a rename.** The file is still `MainMenu.ts` and the phase is still `'menu'`, because the `Phase` union and ~10 call sites already speak that word and renaming them buys nothing a reader of this line does not now know: the docs call it the lobby, the code calls it the menu. Three things the merge had to preserve, each of which is somebody else's requirement: design/20's **one click to gameplay** (`setQuickPlay` still turns the primary button into "start a run now" on a portal, with the mode rows below it unchanged), the **NEW HERE?** badge on TUTORIAL until `MetaState.hasSeenTutorial`, and `setAccountEntry`'s host switch — which is what the account **chip** is: a login entry where a host permits one, a plain name label where it forbids one (design/16, design/20). The row heights were **measured, not eyeballed, and the first version was wrong**: CO-OP and PVP QUEUE started out side by side at half width to buy back the vertical space, and `PVP SOLO QUEUE` needs 169px of label in a 135px button — the same in seven of the eight locales (Polish 187, Russian 186). `viewportFit.test.ts` stayed green through all of it, correctly: it asserts nothing lands outside the *design space*, and a label spilling into the gap beside it is still on screen. That sweep now has a companion that measures each label against its own button, with the icon chip installed, because without one `getUiTexture` answers `undefined` in a unit test and every label centres itself into a layout no player sees. Pointed at every screen rather than just this one (`screens/labelFit.test.ts`) it found **17 overflowing labels across six screens**, most of them older than this pass — see design/17's 2026-09-10 section for the table and the three shapes the fix takes. Two of them were on this screen's own SETTINGS chip — German and Italian, shortened to OPTIONEN / OPZIONI — and one was this pass's own: `Button.redraw()` re-centred an icon button's label on top of its own chip, so any `setFill`/`setBorder`/auto-width `setText` after `setIcon` silently undid the offset (fixed in `widgets.ts`; `LobbyRoutes.setSoloPrimary` would have shipped it on the portal). The maintenance banner's headroom is measured too, on a real page: 55px for the worst legal 140 characters. One type got smaller on the way through: `MatchmakingReturnPhase` is `'menu' | 'squad'` now, since Cancel from matchmaking has nowhere else to land.
- ✅ **The identity gate, and the mid-run meta clobber it was hiding (2026-09-10)** (`client/src/platform/identityGate.ts`, wired in `main.crazygames.ts`) — the same report asked for "a loading state for the hosts that log in silently", and building it surfaced a real ordering bug rather than only a missing screen. On the portal, `portalAuth.start()` ran **after** `game.start()` and after `#boot-loading` was removed, so the menu was interactive while the silent login was still in flight. Two consequences, one cosmetic and one not: the account label paints as a guest and then flips, and — because quick-play makes the first click start a **run** — the login can land mid-run, where `OnlineMatch.syncMetaWithSession` writes `run.setMeta(remote)` with no phase guard at all. `RunLifecycle.beginRun` has by then already spent the staged loadout through `setMeta(clearLoadout(...))`, so the account's server-side blob can restore what the run just consumed. The fix is deliberately two halves, because the gate alone would be a UX change dressed as a correctness fix: **(a)** boot holds the spinner until the host's auto-login has an ANSWER — an account or a guest — under a budget, since a portal SDK that never replies must not hold the boot and "guest" is both the safe answer and the pre-existing state (`design/16`: logging in is never required to play); **(b)** a session that arrives anyway — a player who signs in on the portal page mid-session, which `addAuthListener` exists to catch — has its meta sync **deferred to the next return to the lobby** instead of being applied under a live run. The gate makes the race unlikely; only the guard makes it impossible.

- ✅ **Main menu shipped** (`client/src/game/screens/MainMenu.ts`) — the boot front door design/10 had never actually gotten (the vertical slice used to drop straight into the forge/loadout screen with no way back out to a title screen). Deliberately minimal: PLAY (primary) + SQUAD + LOGIN + SETTINGS. PLAY now opens **Mode Select** (below) instead of Forge directly. **⚠️ Superseded 2026-09-10 — see "The lobby" below.** The file is still `MainMenu.ts` and the phase is still `'menu'`, but PLAY no longer opens anything: the four mode routes moved INTO this screen and `ModeSelect.ts` was deleted. What survives verbatim is this bullet's reason for existing — a front door you can get back OUT to — and its "clutter" instinct, which is what kept the merged screen to five routes and a chip rather than a dashboard.
- ✅ **Mode Select shipped** (`client/src/game/screens/ModeSelect.ts`, 2026-08-03) — **⚠️ Superseded 2026-09-10: this screen no longer exists**; its four buttons are rows in the lobby, and everything below describes why they were needed, which is unchanged. — closes the gap the note above used to describe ("PvP/Arena entry is still a boot-time URL flag, not a runtime choice"). Four buttons: **SOLO PvE** (routes to Forge, byte-identical to the pre-existing path), **CO-OP** and **PVP SOLO QUEUE** (both open Matchmaking below — previously reachable ONLY via the `?online=1`/`?pvp=1` boot flags, which still work for dev/testing), and **TUTORIAL** (below). SQUAD stays a separate MainMenu button (unchanged) for a pre-formed party. A small "NEW HERE?" badge marks TUTORIAL until `MetaState.hasSeenTutorial` — never forced.
- ✅ **Matchmaking shipped** (`client/src/game/screens/Matchmaking.ts`, 2026-08-03) — wraps `connectOnlineSession` (co-op/PvP solo queue AND the pre-formed squad path both route through it now) with a connecting state (elapsed time + Cancel) and an error state (message + Retry + Back). **Since 2026-09-26 ([volume 96](roadmap/96-2026-09-26-coop-party-matchmaking.md)) the connecting state also counts down to the queue's AI backfill** — *"AI players fill empty seats in 4s"*, then *"Filling empty seats with AI players…"* — from the `botFillInMs` matchsvc now returns on every queued answer, and its elapsed time actually runs: nothing had ever called `Matchmaking.update`, so it had read "0s elapsed" for every queue since it shipped (`GameLoop`'s `lobbyScreens` drives it now). Fixed two real latent bugs found while building it: `WebSocketTransport` had no `error`/`close` listener at all, and there was no timeout waiting for `match_start` — a bad ticket or a dropped connection used to leave the player on a blank `playing` screen forever with zero feedback.
- ✅ **Tutorial level shipped** (`client/src/game/match/tutorialConfig.ts` + `TutorialHintController.ts`, 2026-08-03) — a fixed, flat (non-dungeon) 2-floor run teaching move/aim/fire → weapon-swap → melee-deflect → the real checkpoint (the actual `Portal`/`PortalPrompt`, not a scripted stand-in). *Was "the real extract-vs-descend checkpoint DECISION" until 2026-09-10: `ENGINE_VERSION` 61 removed that decision, so floor 0 now teaches DESCEND and floor 1 teaches EXTRACT — the shape of a real run, but no longer a choice. The two floors are kept for exactly that reason; a one-floor tutorial would never show DESCEND at all.*. Always skippable via the pause menu (relabeled "SKIP TUTORIAL" for this run only); completing OR skipping alike sets `MetaState.hasSeenTutorial`. **Since 2026-09-08 the teaching BEATS also run on a first-time player's first real run** (`RunState.firstRunHints`, armed by `RunLifecycle.beginRun` when `!hasSeenTutorial`, gated in `GameLoop` through the renamed `Game.isTeaching()` — the union of the tutorial flag and this one). The standalone level is unchanged and still the only thing `tutorialActive` means; what changed is that the hints stopped being reachable only from a menu route a first-time player has not taken. `design/20` has the reason it became urgent — a game portal lands a new visitor in a run on their FIRST CLICK, so that path was also the path with no control instructions on it. Two consequences of running over a real loadout rather than the tutorial's hand-picked pair: a lesson this loadout cannot teach is skipped rather than waited on (and lands on a silent `done`, not a congratulation), and the wording follows the input device — the single old string named "left stick / WASD" and described an aim stick v33 removed. Exposed and fixed a real bug along the way: the checkpoint-eligibility gate, the HUD's floor chip, and the result screen's floor line all hardcoded `EMBER_DUNGEON.floorCount` instead of reading the run's actual config — harmless while the ember dungeon was the only floored content, wrong for any flat `EngineConfig.floors` run (fixed via a shared `totalFloorCount()` helper, `client/src/game/match/floorCount.ts`).
- ✅ **PvP match preview shipped** (`client/src/game/screens/PvpPreview.ts`, new `'pvpPreview'` `Phase`, 2026-08-03) — sits between ModeSelect's PVP SOLO QUEUE and Matchmaking so a player sees the real map and their own PvP-scaled character/weapon (`buildArenaSpecs` — the same function a real seat is built from) before committing to queue, instead of jumping straight into "Finding a match…" blind. NOT wired into the squad path — see the diagram note above. **Its BACK is an EXIT FROM THE QUEUE, not a navigation — corrected 2026-09-20 after a live freeze report.** This screen is reached only after `OnlineMatch.beginSoloQueue` has already declared the run online (`RunState.online`, set before any screen is drawn), so the two exits are not symmetrical the way the buttons look: QUEUE goes further in, BACK has to undo that declaration. It was wired straight to `nav.showMenu()`, which walked back to the lobby with the flag still set — and `GameLoop.update` routes the WHOLE frame on that flag, so the next OFFLINE run (any of them; the report was the lobby's TUTORIAL row) entered `advanceOnline`, found no session, held the scene and returned. What the player saw was the room `RunLifecycle.enterPrimedRun` had just built with nothing alive in it, a HUD still showing the PREVIOUS run's chips, and — `keydownAction` and `HudView.onPause` are gated on the same flag — no working pause or Escape to get out with. BACK now calls `OnlineMatch.onCancelled`, the verb the Matchmaking screen's CANCEL already used, which clears the flag and the party id and routes to the lobby itself. `RunLifecycle.resetRenderState` also declares every fresh run offline by default (with `finalizeOnlineRun` re-declaring it), so a future screen cannot reintroduce this; `client/src/game/gameRunClock.test.ts` pins the whole seam end to end through `Game`, because every unit test involved was green throughout — the flag is written by one controller, read by a second and leaked by a screen owned by a third, and each of those suites stubs the other two.
- ✅ **Loadout screen shipped** (`client/src/game/screens/Forge.ts`) — the old keyboard-only monospace text board is now real clickable tiles, paged 8-at-a-time (`BLUEPRINT_CATALOG` has more entries than the digit-key shortcuts ever reached), character-cycle arrows, a Clear Loadout button, and a Start Run button. Keyboard shortcuts (digits/arrows/C/X/Enter) still work unchanged as a second input path — both drive the exact same underlying Game methods, not duplicated logic. ✅ **One real gap in that pass survived until 2026-08-05: the "Store (demo: free)" buyable-blueprint line was display-only, with no tap equivalent to the `KeyB` keyboard shortcut — every other Forge action got a real `Button` in the original pass, this one didn't.** Closed with `Forge.acquireBtn` (shown only while `purchasableBlueprints(m).length > 0`, positioned above the blueprint grid so it never overlaps the first card), wired to the exact same `forgeAcquireBlueprint()` method the `KeyB` handler now calls too (extracted from the forge key handler — `controllers/ForgeInput.onKey` since 2026-09-03, `Game.onForgeKey` when this shipped — one source of truth for both input paths, matching every other Forge action's existing convention). ✅ **Blueprint rows became icon cards (2026-08-14, user request: "display them as icon cards" — not a list).** The one-`Button`-per-row vertical list is gone; `ui/BlueprintCard.ts` (new) renders each blueprint as a 4-column grid tile — weapon art on a rarity-bordered chip, name/cost/status stacked below it, a `[n]` key tag, and a staged-count badge — with the browse cursor now a bright border highlight instead of the old inline `»` glyph (a glyph has no "line start" to sit at once rows become a grid). Page nav/acquire/clear button x-positions now derive from the grid's own measured width instead of a hardcoded row-width constant. Same click-to-craft/keyboard-shortcut behavior throughout — presentation-only change, no `MetaState`/transaction logic touched.
- ✅ **Result screen content decided and shipped** (`client/src/game/controllers/RunOutcome.ts` + `Screens.ts`) — was previously a single squashed sub-line; now shows floor reached, materials banked/lost, `Time M:SS` (free from the sim's own `s.tick`/`TICK_RATE`, no new state), and score, plus a secondary "Main Menu" link (the primary confirm/tap still re-enters the loadout screen to gear up for the next run — or Mode Select, for a tutorial run).

- **A screen with real buttons is driven ONLY by those buttons** — since 2026-08-17, with no exceptions at all. Raw controller/pointer *level* input (the `firing` bit sampled per frame) no longer confirms anything on any screen: `GameLoop.ts`'s `pollConfirm`/`prevFire` rising-edge poll is gone, and so is the `confirmEdge.ts` helper that used to gate it (`acceptsFireConfirm`/`shouldConfirmOnFireEdge`, deleted with its test). The **result screens** were the last holdout — the fire edge was the fallback there for `Screens.ts`'s own tap-anywhere handler — and they now carry an explicit CONFIRM `Button` instead, because a stray click or still-held fire from the fight itself could dismiss the results before they were ever read (the same report that flagged an almost-instant swarm death read it as "the level just exited on its own"; see `roadmap/current-state.md`). **Note what that removal cost, if you are adding a phase:** the `Phase` union moved out to `client/src/game/phase.ts`, and the exhaustive `Record<Phase, boolean>` in `confirmEdge.test.ts` — which used to fail type-check until a new phase was explicitly given `false`, and is how `modeSelect`/`matchmaking` and `pvpPreview` (both 2026-08-03) got added — went with it. Nothing fails to compile now; the invariant holds structurally instead, because there is no fire-confirm path left for a phase to opt out of. Rationale for the rule, learned the hard way (2026-08-02): `firing` goes true on mouse-**down**, and a human click holds for ~100ms — several frames — so a per-frame confirm poll fires before Pixi can deliver that button's `pointertap` on the way back **up**, then hides the screen the press started on so the intended tap is swallowed outright. That collapsed every main-menu button into "any click → forge" and every forge button into "any click → start the run". Note this is the *opposite* rule from in-match input, where `08` deliberately edge-detects discrete actions from the button *level* — inside a match there are no competing widgets to lose the race to.
- **A screen drawn over background art owns its own contrast** (2026-08-02). `hub_bg.png` sits behind every menu-shaped screen, and its brightness varies enough across the image that no single semi-transparent button fill reads everywhere on it. So: `Button` fills are **opaque** and may carry a border (`ui/widgets.ts`), and a cluster of controls gets its own dark backing card rather than trusting the art behind it (`MainMenu.ts`). Corollary, from the same report: **two adjacent buttons must differ by more than their label.** LOGIN and SETTINGS stacked vertically with near-identical icon chips produced repeated "the click went to the wrong page" reports even though routing was correct end-to-end — the fix was hierarchy (PLAY primary: bigger + green, the "go" colour `PartyScreen`/`LoginScreen` already used; SQUAD secondary; LOGIN/SETTINGS side-by-side with distinct icon-chip colours), not a routing change.
- **`ScreenManager`** is a small render-side state machine (`enum Screen`, one active `Container` per screen, swap on transition). It owns the `GameEngine` lifecycle: constructs it with `(config, seed, input)` on match start, tears it down on result. It is *not* deterministic and carries no gameplay — safe to hold Pixi objects, timers, wall-clock.
- **Result → restart** is just "build a fresh `GameState` from a new seed and re-enter `playing`." Because a run is `seed + config + input stream` (`08`), restart needs nothing persisted from the old match except meta progression (server-authoritative, `05`/`09`).
- **Pause** (single-player only) stops calling `engine.tick()`; the last state stays on screen. In co-op/PvP there is no true pause — the frame stream keeps coming (`06`); the shell shows a non-blocking overlay instead. ✅ **Shipped exactly as specified** (`client/src/game/screens/PauseMenu.ts`, Escape/P) — offline/local play genuinely freezes (mirrors the hit-stop `acc`-doesn't-accumulate trick, no catch-up burst on resume); online is a documented no-op for now (the overlay itself isn't built for that path, matching "no true pause" above). ✅ **A real touch/WeChat entry point shipped too (2026-08-05) — a genuine gap, not a design deferral: the ONLY way into `pause()` was a keyboard `Escape`/`P` listener, so a touch/WeChat player (no keyboard at all) could never pause mid-run.** `HudView.pauseBtn` (`‖`, top-right corner, above the minimap) calls the exact same `pause()` the keyboard handler does, gated by the identical `!this.online` check (`pause()`'s freeze is unconditional once entered — `phase === 'paused'` skips both `advanceSim` AND `advanceOnline` — so the button replicates the keyboard path's online guard rather than inventing a second one). Lives inside `HudView.view`, so it inherits every phase-transition's existing show/hide for free (no new wiring needed at the ~11 call sites that already toggle `hudView.visible`).
- **A record button beside it, for the same reason (2026-08-31).** `HudView.replayBtn` ('●', immediately left of `pauseBtn`) saves a replay of the run so far, marked at that tick — the second entry point to the verb `design/08` describes under "Getting a replay OUT of a live session", whose first was an **F9 hotkey**, i.e. unusable on exactly the platform a bug report is most likely to come FROM. Two rules it follows that the pause button set the precedent for: it calls the same `saveReplay()` the keyboard does (one verb, never two implementations), and it **hides itself when the run cannot be saved** — online, whose record is the server's confirmed stream, or while watching a replay, which is already somebody else's file — rather than offering a control that silently does nothing. Its outcome is a toast, and all three outcomes (saved, nothing to save, this device cannot) are separate `t()` keys (`17`): a control the player pressed BECAUSE something went wrong must never report a false success.

### The lobby audited as a front door (2026-09-17) — three of its five routes are shut or slow

Stage 2 opened on the front door (`ROADMAP.md` "Product stages"), and the first question asked of
it was about the account chip. Auditing the chip is what surfaced the larger finding: **the five
routes are not in equal health, and nothing on the screen says so.** Work log:
[volume 70](roadmap/70-2026-09-17-home-and-login-design.md).

| Route | What a solo player actually gets today |
| --- | --- |
| SOLO PvE | works |
| TUTORIAL | works |
| **CO-OP** | ~~**a dead door.** `Matchmaker.poll` backfills bots only for `mode === 'pvp'`; a co-op queue with nobody else in it waits out `queueTtlMs` (30 s default) and then EXPIRES~~ — **fixed the same day**, see below |
| PVP SOLO QUEUE | ~~works, but a solo player waits `pvpBotFillMs` (30 s default) before the bot backfill forms the room — i.e. the empty-queue case is answered slowly rather than answered~~ — **5 s since the same day**, see below |
| SQUAD | needs a second human and a room code — the one route still conditional on somebody else, and the only one that should be |

Three consequences, all of them design questions this doc owes an answer to rather than bugs with
an obvious fix: whether co-op should backfill with the bot ally that `?coop=1` already builds
locally; whether an empty queue should be answered in ~5 s rather than 30; and whether a route
that cannot currently be walked through should carry the same visual weight as one that can.
✅ **All three answered the same day — see "Two of the five routes were a server question" below.**

**And the returning player's first need is one screen deep.** CONTINUE RUN (`runSave.ts`, 2026-09-10)
lives in the Forge, behind SOLO PvE, so a player who saved on floor 3 last night sees a lobby that
says nothing about it. A saved run is device-local and does not travel with an account
(`design/16`), which makes its visibility a lobby question and not an account one.
✅ **Fixed the same day — see the next section.**

### CONTINUE RUN on the front door (2026-09-17)

`client/src/game/ui/LobbyRoutes.ts` + `client/src/game/match/resumableRun.ts`; work log:
[volume 71](roadmap/71-2026-09-17-lobby-continue-run.md). The lobby grows a sixth route at the
TOP of the stack — CONTINUE RUN plus a one-line caption naming the floor and the time played —
and it resumes directly, with no stop at the Forge. Five decisions, each of which is somebody's
requirement rather than a preference:

- **The row is drawn off `checkResumable`, not off "a save exists".** Those are different
  questions and the Forge had been answering the wrong one since 2026-09-10: a save from an older
  `ENGINE_VERSION`, or one whose floor library moved under it, drew a full-size primary CONTINUE
  whose only possible outcome was to drop the save and toast a refusal. Surviving one screen deep
  is not the same as surviving on the front door, so `resumableRun.ts` now rebuilds the run config
  from **today's** content for the save's own seed and loadout and asks the same `checkResumable`
  the resume itself will ask. **Both screens read that one function**, so the lobby and the Forge
  cannot end up disagreeing about whether there is a run to come back to. Its cost is real and
  measured — `contentHashOf` digests 20,724 characters of `EMBER_DUNGEON` + `EMBER_L1_ROOMS` —
  so the verdict is memoised against the save object the process-wide slot hands back.
- **A non-resumable save is left in storage, not cleared.** This is a read called from a render
  path (the Forge asks twice per `render()`, on every keystroke), and a provider that mutates
  storage while a screen lays itself out turns a re-render into a side effect. The slot is
  reclaimed by the next `beginRun`. The honest cost: a player whose save we broke is told nothing
  rather than being told at the moment they press a button they cannot use — which is the trade
  design/10 already makes elsewhere ("a refusal a player cannot predict reads as a broken button").
- **On a portal, CONTINUE takes quick-play's slot instead of standing beside it.** Both answer
  "start playing now", the platform requirement behind PLAY is that a **first-time** visitor reach
  gameplay in one click (`design/20`), and a player with an unfinished run is not one — CONTINUE is
  one click into gameplay by the same measure. That is also the only shape that fits: the tallest
  legal lobby with both — portal quick-play, the data notice, a 140-character maintenance banner
  and the CONTINUE block — measured **702px against a 640px design height, in all eight locales**,
  and `viewportFit.test.ts` failed the moment the case was put in front of it. What was NOT done is
  re-point PLAY at the resume: one button whose meaning depends on the state is how a player loses
  a run they meant to keep, the same rule that keeps SAVE & QUIT and QUIT as two rows in the pause
  menu. Two labels, one drawn at a time, is a different thing.
- **The floor and the elapsed time are a caption, not part of the label.** A `CONTINUE — FLOOR {n}`
  button is 316px of Russian in a 280px row; the caption is mostly digits and measures 152-165px
  in the worst locale. It is one unwrapped line on purpose, because wrapping would make the routes
  block's height a `Text` measurement and every position in these screens is arithmetic on
  constants so a layout needs no canvas. `labelFit.test.ts` sweeps buttons and would not have seen
  it, so `LobbyRoutes.test.ts` measures it against the row width in all eight locales itself.
- **Nothing shares a slot.** CONTINUE takes the y SOLO used to sit at and every row below moves
  down by the whole block, so a tap aimed at SOLO on a save-less lobby can never land on CONTINUE
  on a saved one — the pause menu's SAVE & QUIT rule, one screen out.

A refusal that does slip through (the save died between the render and the press) now re-renders
**the screen the press came from** rather than always the Forge: answering "no" with a navigation
is its own defect, and both screens carry the verb now.

### Two of the five routes were a server question, not a UI one (2026-09-17)

`server/src/Matchmaker.ts` + `server/src/BotClient.ts`; work log:
[volume 72](roadmap/72-2026-09-17-coop-backfill.md). The audit above asked three questions and
the third one — the only one that was actually about this document — turned out to be answered
by the other two. **A route in poor health is not a styling problem.** Dimming CO-OP would have
been a change to how the lobby lies about itself; the route was one `mode` check away from
working.

- **CO-OP bot-fills, like PvP always has.** `Matchmaker.poll`'s backfill arm was gated on
  `waiter.mode === 'pvp'`, so the co-op queue's only exit was expiry. It is now gated on the
  wait alone, and the mode selects only the DELAY. The seat count is not a decision: co-op is
  a 2-seat match everywhere the lobby can produce one (`onlineConnect.ts` hard-codes it), so
  "fill every empty seat", the rule PvP already used, is exactly one ally.
- **The ally is the one the game already ships.** `AllyController` has driven the second seat
  behind `?coop=1` since ROADMAP 3.1. That is what made this worth wiring rather than
  designing: the content existed and only matchmaking refused to reach it. `BotClient` now
  picks its brain from `match_start.mode` — `AllyController` for co-op, `PvpBotController` for
  pvp — and builds its EngineConfig with `buildOnlineConfig`, the same function every browser
  tab calls, instead of the arena builder it used to call unconditionally.
- **An empty queue is answered in 5 s, both modes.** `match.pvpBotBackfillDelayMs` came down
  from 30 s, and `match.coopBotBackfillDelayMs` is new beside it. Thirty seconds is a
  matchmaking window and this deployment has no queue to window over — with nobody else
  waiting, those seconds only spelled out the emptiness at greater length. Five is still long
  enough that two people who tap within a few seconds of each other are paired with each other.
- **Two flag names rather than one shared "bot backfill delay".** A PvP bot is a lesser
  opponent and a co-op ally is not, so a deployment that grows a population will want to wait
  for a human in one mode and not the other; one value would make raising the PvP window raise
  CO-OP's too.

**What the audit's third question is owed, now that the first two are gone.** The rule this
doc adopts is: **do not dim a door — open it, or take it off the screen.** A de-emphasised route
is still a route the player taps, and the only thing the styling bought was a worse first
impression on the way to the same dead end. SQUAD is the one remaining route conditional on
another person, and it is not in poor health — it is a feature that needs a friend, which is a
fact to SAY on the screen rather than to imply by weight. Nothing in the lobby's styling
changed in this pass, and that is the finding.

**One thing this does not fix, deliberately**: a queue entry whose client closed the tab still
sits until `queueTtlMs`, and can be seated into a room that then never starts. It is reaped by
age now in every mode (it was reaped in none of them for PvP before this pass), which bounds it;
what would remove it is a cancel on the way out of the matchmaking screen, which nothing sends.

### The card grouped by kind, and TUTORIAL taken off the screen rather than dimmed (2026-09-22)

`client/src/game/screens/MainMenu.ts`, `client/src/game/ui/LobbyRoutes.ts`,
`client/src/game/screens/Settings.ts`, `client/src/game/controllers/{gameWiring,RunLifecycle,
gameAssembly}.ts`; work log: [volume 91](roadmap/91-2026-09-22-lobby-route-grouping.md). The owner
looked at a shipped screenshot and asked whether eight tap targets on one card was too many. The
count was not the defect: the card held three different KINDS of route — start playing
(CONTINUE/SOLO/CO-OP/PVP), prepare (SQUAD/FORGE/TUTORIAL), chrome (LOGIN/SETTINGS) — with nothing
in the layout encoding that, so a player scanning it saw eight near-identical dark pills and the
one green fill.

**LOGIN and SETTINGS left the card entirely**, to a centred row 12px under it with no backing
panel — they are not routes into the game, and the 2026-08-02 legibility fix that paired them
side-by-side (design/10 above, this same doc) had already treated them as a different kind of
control; this pass just stopped drawing them on the same card as the ones that are routes. A 1px
divider inside `LobbyRoutes` now separates "start playing" from "prepare" — full width, no
`onTap`, so it is a rule rather than a button and stays invisible to `widgetOverlap.test.ts`'s
tappable walk without a special case. Height was break-even by construction: the utility row's 42
+ 12px gap that left the card is exactly what the card's bottom pad gave back (24 → 12, sized for
a button's descender room that is no longer the card's last element), and the divider's own 12px
of extra room (8 + 1 + 8, replacing a plain 5px row gap) came out of that same pad. Measured at
the binding case (portal, quick-play + data notice, no save): 630px of 640 before, 630 after.

**This is the "do not dim a door" rule from the 2026-09-17 pass above, taken at its word.**
Grouping by KIND is not dimming by health — every route below the divider keeps its height, fill,
border and icon chip, and the fix is a separator, not a lower-contrast fill — but the rule still
bites on TUTORIAL: `LobbyRoutes.setRecommendTutorial(recommend)` used to drive only the "NEW
HERE?" badge, with the row itself always on screen regardless of `MetaState.hasSeenTutorial`. It
now hides the row too when `recommend` is false, which is the letter of "open it, or take it off
the screen" applied to a route that had been silently dimmed-by-habit rather than styled that
way — a returning player was never being asked to ignore the badge, they were being asked to
ignore the whole row, every visit, forever. Taking the row off the screen would make the route
UNREACHABLE if nothing replaced it, so `screens/Settings.ts` gained a REPLAY TUTORIAL entry (a
fixed action, not a `SettingsState` field) as the second door onto the same run
(`RunLifecycle.beginTutorialRun`, now hiding either screen it might have been reached from). It
joined the existing MUTE/BACK row rather than getting a row of its own: Settings' design height
already sat exactly on the 640px floor (`viewportFit.test.ts`), so a fresh row would have
overflowed it by the row's own height, and a row already wide enough for the worst-case Russian
MUTE/BACK labels had more room to spare than a blank one did.

**TUTORIAL also stopped borrowing the account chip's purple** (`0x6b46c1` → `0x2f6f5f`) — it had
been reusing `icon_account`'s colour as well as its glyph, a second and more literal violation of
this doc's own "two adjacent buttons must differ by more than their label" (2026-08-02, cited in
the 2026-09-17 sweep above). The glyph itself is still borrowed; no dedicated tutorial icon
exists yet.

**Deliberately out of scope**: relabelling CO-OP/SQUAD, the sharpest remaining confusion the
audit that prompted this found (they cross two axes — mode and social unit — and neither label
says which one it differs by). It is a copy change gated on an open design question
(design/05:152, whether co-op PvE is matchmade or friends-only at launch) that a layout pass
should not decide by accident in eight locale files; the new divider already puts the two on
opposite sides of a visible rule, which is most of the confusion this pass could close without it.

### The account chip: clickability is the host's, the copy is the session's (2026-09-17)

`MainMenu.setAccountEntry(boolean)` fuses two decisions that come apart at the first host that has
an identity and forbids a logout — which is every federated host (`design/16`'s platform matrix).
The split to make when this is next touched: the HOST decides whether the chip is interactive, the
SESSION decides what it says, and a host that is still resolving a silent login renders a
placeholder rather than the word "guest" (the `identityGate` already holds boot for exactly that
window; the chip is the part that does not know about it yet). A restored-but-not-yet-revalidated
session must look **identical** to a confirmed one — the player is not being asked to care that we
are checking.

## HUD (in-match)

Lives in the `ui` layer (`01`, topmost). Renders each frame from `state` + `events`. ✅ **The HUD is composed widgets** (`client/src/game/ui/HudView.ts`) — a `PlayerCard` (character portrait + name + HP/shield/energy bars), a `WeaponCard` (real weapon art + rarity border + rarity/kind/element subtitle + element-tinted damage badge + cooldown sweep), a row of icon-led `StatChip`s, an `AllyRow`, a `SeatRoster` (2026-09-08 — who else is in an online match, by name; see below), and toasts, over one backing `Panel` sized to whichever section is widest, plus the `Minimap` floating independently top-right (shared by both modes since 2026-08-05, see below). It shipped as a monospace `Text` blob, then as two `Text` lines over a panel, and was rebuilt into widgets 2026-08-02 (see "Every value on screen is a widget" below).

| Element | Source | Notes |
|---|---|---|
| Health | `player.hp / maxHp` | ✅ **Shipped** as `PlayerCard` (`ui/PlayerCard.ts`): HP bar + a separate shield bar (`07`'s two-pool — hidden entirely for a zero-shield body, never an always-empty track), both flashing on decrease (`ui/widgets.ts`'s `Bar`). Carries the character's own portrait, reusing the rig bundle's `shell` art (`render/skinRegistry`) rather than commissioning headshots — a new character needs no extra asset to appear here. **A third bar joined it 2026-09-05 (`ENGINE_VERSION` 59): weapon energy** (`03`'s ammo economy), slim and unlabelled like the shield's and hidden the same way at a zero pool, in the same teal as the `energy` DROP so "the teal thing on the floor refills the teal bar" needs no explaining. It sits on the PLAYER card rather than beside the weapon even though it is a weapon cost: it is a POOL, it belongs to the player and not to whatever is in their hands, and on the weapon card it would look like it emptied when you swapped. Unlabelled because the number is never the decision — the decision is "can I keep firing this", which a fraction answers and a count does not. |
| Equipped weapon + slot | `player.weapon` (`02`) | ✅ **Shipped** as `WeaponCard` (`ui/WeaponCard.ts`): the SAME business-end texture the rig mounts in-world and the Forge rows use (`render/weaponSkins`) on a rarity-bordered chip (`14`'s border-not-hue rule), a `rarity · kind · element` subtitle, a damage badge tinted by `damageType` (`13`'s locked element palette), and the cooldown sweep off `weapon.cooldownTicks`. **The idle second slot is now represented too, 2026-08-12** (user request, annotated screenshot): a small dimmed `WeaponSlotChip` (`ui/WeaponSlotChip.ts`) sits immediately right of the active `WeaponCard`, hidden below a two-weapon loadout. **That hidden state is now a defensive fallback rather than a state normal play reaches** (`ENGINE_VERSION` 45): every loadout is a gun + a melee weapon in both modes (`resolveLoadout` for PvE, the landing-kit pair for PvP), because the self-hiding chip was exactly what made a one-weapon regression invisible — a run shipped with no second slot, and nothing looked wrong. `PvpPreview` draws the same widget pair, so the pre-match preview and the in-match HUD read identically. Tapping it swaps the active slot — same `CommandBuilder.requestSwap()` the `05` corner buttons and keyboard `[1]`/`[2]` already drive — since a two-weapon loadout makes "tap the idle slot" and "cycle the active slot" the same action; see "Camera cover-fit + weapon-slot HUD chip" in `ROADMAP.md`. |
| ~~Ammo / charge~~ | — | **No such stat exists** (noted 2026-09-03): `RangedSpec` has no ammo, magazine or charge field, and no weapon has ever had one — a cooldown is the only rate limit in the game. Kept as a struck row rather than deleted so this table cannot be read as promising it; if a charge weapon is ever authored, `WeaponCard`'s cooldown sweep is where it goes. |
| Swing / parry flash | `deflect` event (`08`) | Transient — a melee swing that deflected a bullet; no persistent "block" state exists (`05`) |
| Minimap / room progress | `room` + cleared count (`05`/`09`) | ✅ **Shipped — one shared widget for both modes since 2026-08-05.** `client/src/game/ui/Minimap.ts` reads `content/arenas.ts`'s room/door graph; PvP feeds it `state.arenaMap`/`state.zone` directly, PvE converts `state.dungeonRooms`/`dungeonDoors` into the same `ArenaMap` shape via `dungeonToArenaMap` (`minimapLayout.ts`) and tints rooms via `dungeonRoomStatus` (`safe`/`danger`/a new `unvisited` bucket PvP's zone-driven `roomStatus` has no equivalent for) instead of a zone read — `Minimap.update()` takes that resolver as a parameter now, rather than hardcoding PvP's zone semantics. Both modes also show other online players' rooms as colored dots (green=local, light=remote alive, dark=remote downed). Retired the PvE-only `FloorProgress` (`client/src/game/ui/FloorProgress.ts`/`floorProgressMath.ts`, deleted) — its plain-language legend history (shipped 2026-08-01, folded into the dot shapes themselves 2026-08-02) is moot now that PvE gets the real spatial map: a floor with a fork's real sibling rooms (design/05, 2026-08-05) has genuine 2D structure a linear array-index track couldn't represent (an untaken sibling read as a phantom step, or "done" once its index passed) — the exact gap the shared minimap closes. |
| Extraction / checkpoint prompt | flat mode: `wavesExhausted` + `enemies.length`; dungeon mode: the floor's capstone room `activated && !hasLiveEnemy` (`ExtractionSystem`, `client/src/game/match/floorCount.ts`'s `checkpointReached()`) | ✅ **Shipped, rewritten again 2026-08-02**: the 2026-08-01 centered "HOLD [E] / TAP [E]" text banner is gone — a real world-space `Portal` (`client/src/game/scene/Portal.ts`, a glowing Graphics gate, no new art) appears at the checkpoint room's center once the mode's own condition is met; walking within a proximity radius of it shows `PortalPrompt.ts` (Panel + two `Button`s: "Bank & Extract (N materials)" / "Descend to Floor N+1"), replacing `HudView`'s old `checkpointPanel`/`checkpointText` entirely. Engine-side, the hold-to-extract/tap-to-descend `INTERACT` timer (`EXTRACT_HOLD_TICKS`) is gone too — the popup's buttons drive two explicit one-shot `Button.CONFIRM_EXTRACT`/`CONFIRM_DESCEND` presses (`CommandBuilder.requestConfirmExtract()`/`requestConfirmDescend()`, same one-tick-pulse latch shape as `SWAP_WEAPON`), resolved by `ExtractionSystem` directly — no more hold-duration bookkeeping (`ENGINE_VERSION` 31). A popup click also can't accidentally fire a shot (`CommandBuilder.suppressFire()`, gated on `PortalPrompt.isOpen` — harmless here because the room is already clear whenever this popup can show, which is exactly why the weapon-pickup panel, which opens mid-fight, stopped sharing this mechanism on 2026-09-02 and swallows its own presses instead; see `03`) — see `daydayup-hud-legibility-pass-2026-08-02` memory (addendum) for the `WebInput.ts` raw-`mousedown` snag that made this necessary. **Client bug, fixed 2026-08-12:** the 2026-08-04 "Room & door model" pass rewrote `ExtractionSystem`'s dungeon branch to the capstone-room check but left this row's client-side gate reading the old `wavesExhausted` flag (never set in dungeon mode) — the portal/popup never appeared on a non-final floor. See "Live-play bug-fix pass" in `ROADMAP.md`. **Client bug + VFX redesign, 2026-08-12 (follow-up):** the portal was actually centered on the whole floor's bounding box, not the capstone room's own rect — on a multi-room floor it could land in a corridor or on a wall. Also rebuilt `Portal.ts`'s look (a standing arch, a two-ring counter-spinning vortex around a bright core, and inward-spiralling particles) so it reads as a real gate rather than a static ring + glow. See "Portal placement + VFX pass" in `ROADMAP.md`. **Gameplay bug, fixed 2026-08-12 (follow-up):** the LAST floor used to skip this whole row entirely — `ExtractionSystem` auto-resolved EXTRACT the instant the capstone cleared, so the portal never opened and this popup never appeared there, leaving no time to walk over to the boss's own death drops. The last floor now opens the same portal + popup as any other checkpoint; `PortalPrompt.ts` just hides its Descend button (no next floor to descend to) instead of showing both. See "Boss-room instant-extract bug fix" in `ROADMAP.md`. | **One button, not two, since 2026-09-10 (`ENGINE_VERSION` 61, `05` "Only the boss floor ends a run"):** an interior floor's popup shows Descend alone and the boss floor's shows Extract alone — the mid-floor extraction option is gone, and `ExtractionSystem` ignores whichever button the popup is not drawing, so a stale click cannot resolve something the panel never offered. The panel shrank with the choice (150 -> 112 px; `FloorCardPrompt` stacks off `screenPx.h * 0.6`, this panel's TOP, so the card panel did not move). The Extract label also changed what it counts: the WHOLE carry-out (floor buffer + banked bag), not just this floor's buffer, because that press is now the run's only exit and the bag is at risk right up to it.
| Floor-card offer (pick one of three) | `state.floorCardOffer` (three ids, rolled by `ExtractionSystem` at the checkpoint) + every seat's `PlayerActor.cardVote` | ✅ **Shipped 2026-09-05** (`ENGINE_VERSION` 58, `05`'s loot-economy section): `ui/FloorCardPrompt.ts`, three tappable cards on a Panel sitting **above** the portal popup and driven off the exact same "cleared, and standing at the portal" condition — computed once in `controllers/checkpointOverlays.ts` so the two panels can never disagree about whether the floor is finished. Non-blocking, like every other in-match overlay: the sim keeps running behind it, because lockstep cannot pause for one player (`06`). Card text is `tName`'d off the engine catalogue (`FLOOR_CARDS`, open-ended content keys, `17`) with the magnitudes interpolated from `RUN_BUFFS` itself (`floorCardDescVars`) rather than written into the locale strings, so a retuned buff cannot leave eight locale files promising the old figure. **In co-op each card shows a live vote count and the local seat's own pick is drawn selected** (border, not fill — the browse-cursor convention `BlueprintCard` already uses); solo hides the counts, since there is no majority to resolve. Counts are hidden rather than absent on purpose: "whichever card the most people chose wins" is a rule whose outcome has to be visible *before* it resolves, not after. A tap is a **vote**, never a descend — the portal's own Descend button still leaves the floor, and it now holds until somebody has picked. Presses are swallowed via `onPressStart` → `CommandBuilder.suppressFireUntilRelease()`, the `WeaponPickupPrompt` mechanism rather than the `suppressFire`-while-open one this row's popup uses, because a card tap is a click on a panel rather than a state the whole button should be gated on. |
| Shop counter | `state.shops` + the local seat's `PlayerActor.coins` (`05` "Shops") | ✅ **Shipped 2026-09-14** (`ENGINE_VERSION` 64): `ui/ShopPrompt.ts`, deliberately the same object as the weapon-pickup panel one row up — a list of things in reach, and **tapping a row IS the purchase** (`CommandBuilder.requestShopBuy` → `PlayerCommand.shopBuyId`, the one-shot latch `pickupTargetId` established; `ShopSystem` does the authoritative check). Non-blocking like every other in-match overlay, and with **no close button on purpose**: walking off the counter's mat closes it, and the mat is drawn (`scene/ShopLayer.ts`) at exactly the radius the sim gates on, so "the panel is open" and "a tap will be accepted" are the same condition rather than two rules that can drift (`ui/shopProximity.ts` holds the shared number). **Three row states, because a refusal a player cannot predict reads as a broken button**: affordable, too expensive (dimmed fill and price, still tappable — a dead row cannot tell you *why* it is dead, so the row goes `'silent'` and the caller plays `ui.denied` off the refusal, the same outcome-decides-the-sound rule the forge's craft rows already use), and sold (relabelled, kept on the counter so a player who watched a teammate buy it can see where it went). Prices are read off the offer the sim will charge, never re-derived from `SHOP_PRICES`. Presses are swallowed via `onPressStart` → `suppressFireUntilRelease()`, the `WeaponPickupPrompt` mechanism, since a counter can sit in a room that still has enemies in it. |
| Big-chest caption | `state.chests` (`05` "Chest rooms") | ✅ **Shipped 2026-09-15**: `ui/ChestPrompt.ts`, a bottom-centre caption for the BIG chest only — a live `{on}/{total}` count of its mechanism plates, shown from the ring outward so it survives the player walking out to a plate to do the thing it asked. **It started the same day as a SMALL chest's *"Press E to open"*, and that half is deleted**: the owner's answer to the report below was to make a small chest open on approach (`ENGINE_VERSION` 66), so its caption could only ever have flashed for the frame before it opened. Both halves exist because the mechanic had shipped with **no way at all to learn it** — a chest has no art and no sound cue, and the tutorial's hint list teaches move / attack / swap / deflect and stops, so `INTERACT` is named nowhere. Reported as *"the chest will not open"* about a chest that, verified the same day in the running client, opens on the first frame a key reaches `WebInput`. Deliberately **not** shaped like the shop/weapon panels beside it: a chest has no command of its own at all — no chest kind reads a button now — so a tappable row would promise the sim something it never receives; `eventMode = 'none'`, and every press falls through to the weapon. The range is the mechanism ring plus a plate radius (`ui/chestProximity.ts`), which is geometry about the plates rather than a reach to the chest. |
| Pause menu | `phase` (single-player only) | ✅ **Shipped** (`client/src/game/screens/PauseMenu.ts`) — Escape/P keyboard entry point, plus a real `HudView.pauseBtn` for touch/WeChat (2026-08-05, closes a real gap — see Screen flow above; corrects this row's own prior "settings-button entry point" claim, which was never accurate: `Game.ts`'s `settingsBtn` is shown only in the FORGE phase, never during `'playing'`). **Since 2026-09-10 (`ENGINE_VERSION` 61, `05` "Only the boss floor ends a run") it carries a fourth row, SAVE & QUIT**, shown only for a run `match/runSave.ts savableRun` admits — single-player, offline, real dungeon. Plain QUIT stays beside it rather than being replaced: the two are different decisions (keep the run vs. throw it away), and one button whose meaning depends on the mode is how a player loses a run they meant to keep. RESUME/SETTINGS do not move when the row appears (the stack grows downward) and QUIT shifts a full row down rather than sharing a slot, so a tap aimed at one can never land on the other. `ScreenNav.savable()` decides, from the run flags plus the one fact only the sim knows (`dungeonEnabled`); the screen decides nothing, same as the SKIP label above. |
| Pickup / buff toast | `pickup` event (`08`) | Transient "picked up X"; drives the roguelite build feedback — weapons + run buffs, no affixes (`05`/`14`) |
| Run/arena state | `state` (both modes) | ✅ **Shipped** as a row of `StatChip` pills (`ui/StatChip.ts` + `ui/hudIcons.ts`): PvE shows FLOOR / ROOM / FOES / BANKED / COINS / SCORE, PvP swaps in ZONE (stage `+escalation`) / ALIVE / SCORE, and BUFFS joins either row only while the run actually has one. **COINS joined 2026-09-14** (`05` "Shops") and reads the LOCAL seat's own wallet, never a party total — coins are per-seat, so a squad sum would be a number the player cannot spend; its glyph is the struck disc `Pickup` draws for a coin, separated from BANKED's crystal by FORM rather than hue since both are warm. Each chip is a tinted vector glyph + a short localized label + the value; icon tints are taken from what the stat refers to **on screen** (foes take the enemy faction red, banked takes the material-pickup yellow, buffs take the buff-pickup violet, the zone stage takes the amber the `Minimap` telegraphs a closing room with), so a colour learned in the world reads in the HUD for free. **The FOES glyph changed 2026-08-25**: it was a skull, and `13`'s locked element-icon list assigns the skull to POISON, so two different meanings for one silhouette would have shared a screen. The chip took a single-eyed crystal critter head instead — more on-fiction anyway (`13` describes enemies as "living crystal, single glowing eye"; nothing in this world has a skull, which is exactly what makes a skull read as "toxic" rather than as "a mob") — and the element badge keeps the locked glyph. `elementIcons.test.ts` pins both sides of the resolution: the poison glyph has two off-centre sockets, the FOES chip one centred eye. |

- **Damage/feedback numbers, hit flashes, deflect sparks** are driven by the `events` queue (`08`), consumed once per render frame — same channel the fx layer uses (`01`). UI feedback and fx read the same events; neither writes back.
- **Floating damage numbers** (✅ 2026-09-26, [volume 95](roadmap/95-2026-09-26-damage-numbers.md)). Every `hit` prints its `damage` over the target: on every enemy, on this seat, and on another player only when a player hit them (a PvP rival taking fire — not a co-op teammate taking an enemy's, which the ally row already summarises). Colour, in priority order: shield cyan when the shield swallowed the hit whole, red for damage this seat took, slate for the zone and hazard tiles, otherwise the weapon's element (`elementColor`, physical in the locked neutral). A second hit on the same target in the same colour within **150 ms** adds into the number already rising and pops it, so a stream reads as a ticking total and a slow weapon as separate hits. A number lives 750 ms: ease-out rise of 26 screen px, fade over the last 250 ms, sized in SCREEN px (22 px per digit, up to 1.35x for a four-digit hit) so the 3-4.5x room zoom does not blow it up. The shrinking zone needs no case of its own: `EnvironmentSystem` deals a tick through `takeDamage`, which already announces it as a `hit` with `faction: 'environment'`, and numbering `zone_damage` as well would print every tick twice. The on-screen count is capped by the quality ladder (`damageNumbers`: 40 / 28 / 16), and a full screen reuses its OLDEST number, never drops the newest. Built as the sprite atlas the open question below decided: `tools/digit-atlas/gen_damage_digits.py` renders the ten digits of Rubik Bold (OFL-1.1), plus "+" and "!" since volume 101, into `client/public/ui/damage_digits.png` (720x62, 16 kB, white fill under a 5 px dark outline, one shared cell and baseline measured over the digits alone) and writes the glyph table beside it as `render/damageDigitAtlas.ts`; both are committed, so neither the build nor CI needs Python or the font. Drawn as pooled plain `Sprite`s tinted per number on their own world layer `layers.numbers` above `hud` (not a render group — it churns on every hit, and `hud` is one because it almost never changes), so every number on screen is one texture and one batch. Code: `fx/damageNumberModel.ts` (merge, cap, curve — pure), `fx/DamageNumbers.ts` (sprites and pool), `controllers/damageNumberReactions.ts` (which hit, what colour, where — pure). **Crits and heals** (✅ 2026-09-26, [volume 101](roadmap/101-2026-09-26-crit-heal-numbers.md)). A `hit` carrying `crit: true` prints as "N!" in gold (`0xffb020`), 1.4x bigger. Gold ranks below shield cyan and self red, so a crit on you or into a shield keeps that colour but is still big and still has its "!". A `heal` event prints as "+N": heal green for health, shield cyan for a battery, over the local seat only. The engine emits `heal` from `restoreHp`/`restoreShield`, which report the amount after the clamp; they cover the potion, the battery, the shop's heal and shield lines, and lifesteal, but not idle regen or a revive. Style is part of the merge key, so a crit never folds into the plain stream beside it. The narrow "!" has its own advance (`bangAdvance`) so it sits against the digits.
- **HUD never reads a weapon's internal cooldown by calling into engine logic** — it reads the plain `cooldownTicks` field off state and maps it to a 0..1 sweep. Presentation only.
- **Every value on screen is a widget, not a formatted line** (2026-08-02, locked). Concatenating state into a monospace string is the cheapest way to *display* it and the most expensive way to *read* it: `blaster [common] (ranged) dmg 1` and `juggernaut  Floor 1/3  Room 1/2  Enemies 1  Banked 0  Score 0` were technically complete and read as debug output — the user's own report was to point at the corner and ask what it was. A value gets an icon, a colour, and its own box; a run-on line is a bug report waiting to happen. Corollary: **the widget must show the same art the world shows.** The weapon chip mounts the exact texture the rig mounts and the portrait is the rig's own body art, so "the thing in my hand" and "the thing in my HUD" are visibly one object — a name string never established that.
- **The local player must be identifiable in the world, not only in the HUD** (2026-08-02, locked). Giving every actor a floating health bar (2026-08-02's earlier pass) made the player's own bar indistinguishable from a mob's, and the next screenshot report was a player circling their own character to ask what it was. `Actor.setLocal()` (driven by `Scene.reconcile`'s already-resolved local seat) marks exactly one actor with a teal health-bar outline, in `THEME.colors.player` — the one hue no enemy tint ever takes. A teammate does NOT get it: the marker answers "which one am I", not "which ones are friendly". This originally paired the outline with a teal ground ring at the feet too; dropped 2026-08-14 after two more screenshot reports in the same lineage — first that the ring's ground-plane zIndex left its top half hidden behind the body sprite ("looks like a half-drawn shield"), then, once that was fixed by drawing it in front, that a fully-visible cyan ring around the body was now genuinely hard to tell apart from `EnergyShieldFilter`'s cyan rim-glow (`01`). The outline alone never overlaps the shield glow's screen space, so it's the marker now. Since 2026-08-21 that outline is a filled 1-px **contour** around the bar rather than a stroke on it — same hue, same screen space, same guarantee, and it survived the bar's own restyle below unchanged in intent.
- **An in-world bar must render at a value the world behind it cannot move** (2026-08-21, measured). The floating health bar is a HUD readout that happens to live in the world layer, so it is drawn procedurally (`scene/healthBar.ts`) — but its first version was a near-black track at `alpha 0.85` with a 1-px near-black stroke, and being translucent made its rendered luma a function of whatever the actor was standing in front of. Sampled over 18 placements in one real room, the empty track came out anywhere from 25.6 to 57.1 luma and its separation from the surface behind it fell to **1.6-2.9 luma** over the dark floors mobs actually stand on — so at 1/3 HP the player saw a coloured stub with no visible remainder, i.e. no length to read the fraction against. The rule that replaced it: **every layer carrying the bar's silhouette is opaque, and the bar carries its own two-tone frame** — a near-black contour that reads against anything bright (measured wall caps run 72-88 luma, pillar tops 87) plus a light top bevel that reads against anything dark. No single value can separate from both ends of that range; two always can. Re-measured after the change by swapping the surface behind one live bar across 0-197 luma: track and bevel held at **39.5 and 117.3 with zero spread**, and the worst-case cue separation went from ~2 to **55.6 luma**. A non-zero HP also always draws at least one pill-width of fill, because below that it went sub-pixel at gameplay zoom and an almost-dead actor was indistinguishable from a dead one.
- **An in-world readout holds still while its actor does not** (2026-09-21, locked). Report: *"can the health bar above the character stop bobbing up and down with it? my eyes are swimming"*. Every hovering archetype carries a render-only idle lift (`scene/actorLift.ts`'s `HOVER`, `Entity.visualZ`) that `Entity.applyTransform` folds into the drawn `y`, and the floating bar — positioned off that same `y` since the days it was a child of the actor — rode all of it. The numbers are what make it a rule rather than a taste: `amp` is 1-2 world px, so the bar travelled 2-4 px peak to trough against a track `Actor.setHealth` draws **4 px tall**, i.e. **a full bar height, twice per cycle**, or 14-16 screen px at the 3.4-4.1x a room is cover-fitted to. The hover itself is correct and stays — its whole job is the SHADOW shrinking and sliding under a body that left the floor, which is the one thing an authored `idle` clip cannot do. It is just not an argument that reaches the readout: **a bar is a HUD element that happens to be drawn in the world, so it tracks the actor's GROUND position, not its drawn one** (`Actor.applyTransform` adds `visualZ` back; engine `z` is still honoured, so a knock-up would carry the bar with it). The status aura is the counter-example that fixes the boundary — it wraps the body and rides the hover, because it is a property OF the body rather than a value being read off it. What inverts is which quantity stays constant: the bar's screen height, rather than its clearance above the head.
- **Who else is in the match, by name** (2026-09-08, `ui/SeatRoster.ts`) — one line in the HUD's
  left column, drawn only when a seat actually has a name, i.e. only in an online match with at
  least one logged-in player. `design/20` has the requirement it satisfies (a game portal requires
  the platform's own usernames be shown so players can recognise their friends) and the wire half;
  three UI decisions belong here. **Not nameplates over the actors**: a nameplate tracks a world
  position through the camera every frame and then fights the occlusion x-ray for legibility
  (`01`), and at this game's zoom a four-seat room is four labels over four 20-pixel sprites — the
  roster answers "who am I playing with" once instead. **Unnamed seats are omitted, not filled
  in**: a guest and a bot have no name, and `P3` would be a name this game invented and then showed
  to other people as if it were theirs. **Not the `AllyRow`**, which was the obvious place and is
  the wrong one — that row is drawn only for a LOCAL bot ally or the arena harness (`showAlly` is
  `isCoop() || isArenaDemo()`, and an online co-op match sets neither), so a name put there would
  never be seen in the only situation a name exists in.
- **HUD layout math never touches canvas text measurement.** Every width comes from `ui/textWidth.ts`'s `estimateMonoWidth`, never `Text.width`/`Container.getBounds()` — those need a live 2D context, which this repo's plain-node vitest doesn't have, and re-measuring every frame in the browser is wasteful besides. That estimate is **East-Asian-width aware**: a naive `length × 0.6` measured every `zh` string at 60% of its real width, so every panel sized from one came up short in Chinese and looked fine in English.

## Controls & the input→command boundary

The concrete shape of `05`/`04`'s control scheme — one move stick, no aim (v33) — and where `08`'s `PlayerCommand` is assembled.

- **One move stick + a fire zone + corner buttons** (touch), or **WASD + left-click + keys** (web). Both feed one `readController()` that returns a normalized frame: `{ moveDir, moveMag, fire, swap, interact }` (no `aim` field at all — `10` v33 — and no `block`/`jump`, parry is the melee swing, jump removed).
- **Quantization happens here, once:** `moveDir → moveBrad`, `moveMag → 0..255`; buttons packed into the `buttons` bitfield (`08`). The result is a `PlayerCommand` submitted via `input.submit(cmd)` for the current tick. **After this line there are no floats and no pixels.**
- **There is no aim input to unify** (`10` v33, superseding the original "aim modes unified" scheme below): a fired shot's direction — and a melee swing's hit-arc — is `PlayerActor.facing`, computed by the engine's own `ApplyInputSystem` every tick from the nearest hostile, else the movement direction, else held (idle-hold default, `08`). This used to be manual aim (mouse `point` converted against camera offset, or joystick `dir`, both collapsing into `aimBrad`) — that whole seam is gone, not merely defaulted off, because a manually-aimed shot fighting an auto-lock read wrong (see the aim-history note further down this doc).
- **Discrete actions are edge-detected in the engine**, not fired from the UI: the UI only reports the current button *level* in `buttons`; the engine compares against last tick to detect a tap (`08`). So "tap to swap weapon / interact" needs no special UI event — just set the bit while held.
- **Web vs WeChat** differ only in how `readController()` gathers raw input (`WebPlatform` keyboard+mouse vs `WeChatInput` touch move-stick+fire-zone, `04`); the quantization and command assembly are shared.
- ✅ **Touch/WeChat's `interact` field is real now (2026-08-05) — previously a genuine gap, not a deferred design item: `TouchControls.read()` hardcoded `interacting: false` unconditionally, with no on-screen control at all.** This meant a touch/WeChat player could never hold the revive channel (`ReviveSystem` reads `player.interacting` every tick — a sustained hold, same as the keyboard's `KeyE`/`Space`) for a downed teammate. The extraction/descend gesture this field used to ALSO gate was already unaffected by the gap — it moved to `PortalPrompt`'s own tappable `Button`s (design/10, 2026-08-02), which touch could already reach via Pixi's own pointer event system. Fixed: a third held (not tapped) corner button, `TouchControls.ts`'s `interactBtn` — same shape as the fire zone (a hold, not a one-shot latch), tinted `pickupHeal` green in `TouchControlsView.ts` to read as a distinct "supportive" action rather than another weapon-swap tap.

## Layout, orientation & safe areas

- **Landscape, anchored corners.** One move stick on the left half, the whole right half a fire zone (there is no second stick — v33), the weapon/INTERACT buttons clustered in the fire-side top corner (thumb reach), HUD status top-left, minimap top-right (`05`). *(This line described two sticks with the action buttons "by the right stick" until 2026-09-03.)* Everything positions relative to screen edges, re-laid-out on resize, and `setMirrored()` swaps the two halves for the left-handed layout.
- **Safe-area insets.** WeChat and notched phones report insets (`wx.getWindowInfo` safe area; web `env(safe-area-inset-*)` is unavailable inside canvas — read from the platform layer). UI anchors respect them so sticks/buttons aren't under a notch or the WeChat capsule menu (top-right — keep that corner clear).
- **Resolution independence.** UI uses the same world/screen scaling as the renderer; size widgets in logical units and scale with `renderer.resolution` so touch targets stay thumb-sized on high-DPI phones.

## WeChat / rendering constraints (from `04`)

- **`Text` rasterization** goes through the adapter's `createCanvas` → `wx.createCanvas()` (`04`). Fine for HUD labels; **prebake or limit dynamic text** — re-rasterizing large changing strings every frame is a known cost. Prefer sprite-based numbers for fast-changing values (damage, ammo) over live `Text`.
- **No DOM widgets, no eval** (`04`): buttons/sticks are Pixi hit-areas with `eventMode:'static'`, not HTML. Any procedural texture uses Pixi `Graphics`, never `document.createElement('canvas')`.
- **One canvas / WebGL** (`04`): UI shares the single renderer; no second context for an overlay.

## Relationship to the other docs

- **`00`/`01`:** all-Pixi UI, `ui` layer topmost. This doc fills in what lives there.
- **`04`:** WeChat input (the virtual move stick + fire zone) and `Text` constraints; this doc is the cross-platform UI form of it.
- **`05`:** controls, landscape lock, the run loop the screen flow wraps, preset/loadout pick.
- **`06`/`08`:** the engine↔render split and `PlayerCommand`; UI is strictly downstream + the one command channel. Pause/stall behavior follows `06`.
- **`09`:** weapon/preset/rarity data the loadout screen and HUD icons read.

## To design

- ~~**Widget kit:** a minimal Pixi UI component set (button, stick, bar, toast, panel) — build vs. a tiny in-house layer.~~ **Resolved (5.2, shipped):** a small in-house kit, not a framework — `client/src/game/ui/widgets.ts`'s `Panel`/`Bar`/`ToastQueue`/`Button`/`Slider`, used throughout the HUD, `Settings.ts`, and `PauseMenu.ts`. **The kit also owns the UI sound (2026-08-30, `11`'s "The UI cues"):** `Button` plays `ui.tap` by default — so every button in the client is audible without opting in — and takes `sound: 'ui.back' | 'ui.toggle' | 'silent'` where the press means something else; `Slider` ticks once on release, through the bus it just changed, which is what makes the volume sliders auditionable. The cue plays AFTER the button's own handler, because the mute button is one of these and its handler is what applies the new volume.
- **HUD data contract:** the exact read-only view of `GameState` the HUD needs, so it never reaches into engine internals (mirror `08`'s interpolation-snapshot idea for UI).
- ~~Loadout/preset screen data — how much detail (stats, previews) to show.~~ **Resolved
  (5.2):** a forge compare card (`client/src/game/ui/compareCard.ts`) diffs the browse
  cursor's blueprint against whichever loadout slot shares its weapon kind (empty
  loadout falls back to the auto-equip pair) — damage (post-rarity-quality), damage
  type, and kind-specific handling stats (fire-rate/spread/speed for ranged;
  swing/arc/reach/deflect for melee), leaning on handling over raw damage per `03`/`14`'s
  "rarity edge is mostly handling, never crushing." ~~`ARENA_PRESETS` previews remain
  undesigned — PvP preset-pick has no UI yet (`15`).~~ **Resolved (2026-08-03):** the gap was
  narrower than a picker — only one preset/map exist today — so `game/screens/PvpPreview.ts`
  is a confirm/preview step between the lobby's PVP SOLO QUEUE row and Matchmaking, showing the
  real map and the player's PvP-scaled character/weapon via `buildArenaSpecs`. A second
  preset later is additive here, not a rewrite.
- ~~**Settings** (volume once audio lands `11`, control layout/left-handed mirror, quality tier per `01` roadmap).~~ **Resolved (5.2, shipped):** `client/src/game/screens/Settings.ts` — master/SFX/music sliders + mute, built on the widget kit above, wired to `SettingsState`/`AudioBus`. **Left-handed control layout also resolved (2026-08-03):** `SettingsState.controlLayout`, a `Settings.ts` toggle, and `platform/TouchControls.ts`'s `setMirrored()` (swaps the move-stick and fire-zone halves + the weapon-button corner — "move/aim sides" as this read until 2026-09-03, from before v33 removed aim). **The quality tier shipped 2026-08-25** — this line read "a quality tier remains undesigned" until 2026-09-08, three volumes after it landed: a QUALITY row cycling `auto`/`high`/`medium`/`low`, labelled with what `auto` actually resolved to (`AUTO (MEDIUM)`), see `01`'s quality-tier table. **And a FRAME RATE row, 2026-09-08** (`60`/`30`), the battery knob a quality tier cannot express — 30 is one render frame per 30 Hz sim tick, so it halves what a fight costs without the sim and the screen drifting out of phase (`01`'s power budget, `06`'s tick note). **And a REDUCE MOTION row, 2026-09-22** (`ON`/`OFF`, `减少画面抖动`), from the dizziness report in `01`'s frame-pacing section: it suppresses camera shake — ±14 px of white noise applied to the whole world layer, re-rolled every render frame — and the chromatic-aberration pulse, and nothing else. Not the vignette (static), not hit-stop (a pause is the opposite of motion), not any animation attached to an object the player is tracking: those either carry information or stay put, and switching them off would be a different setting nobody asked for. Off by default, because the shake is part of how the game is meant to feel — but a player who needs it off needs it off permanently, which is what persisting it is for. All three rows are presentation-only and none reaches the sim.
- ~~**Result/summary content:** what a run summary shows (drops collected, rooms cleared, time) — ties to `05`'s reward structure.~~ **Resolved, shipped** — see the Screen flow section above.

## Open questions

- ~~**Damage/feedback numbers as `Text` vs sprite atlas** on WeChat — measure re-rasterization cost on the lowest base library (`04`) before choosing.~~ **(DECIDED 2026-09-26, by the owner: a sprite atlas.)** Live `Text` re-rasterization is ruled out on cost without waiting for the measurement. Recorded with it, because this question read as if the numbers existed: **no floating damage number was drawn** (true until the same day's build, below) — `EventReactor` handles `hit` without reading `e.damage`, and `zone_damage` is not handled at all; the two comments that say "the damage number" (`fx/FxController.ts`, `render/motion.ts`) describe nothing. The build: one white-fill, dark-outline digit atlas GENERATED by a build script from an OFL font (not image-model art — ten glyphs need a shared advance width and baseline, and colour comes from a runtime tint so one sheet serves every damage type), drawn as pooled plain `Sprite`s rather than `BitmapText` so the deliberately-unloaded BitmapFont chunk (`12`, `build/runtimeChunkPreload.mjs`) stays unloaded. **Built the same day** ([volume 95](roadmap/95-2026-09-26-damage-numbers.md)) — see "Floating damage numbers" above.
- ~~**On-screen aim on touch:** free right-stick aim vs. aim-assist/snap to nearest (casual-first, `05`/`06`) — decide against real thumbs on a device (`04` checklist item 5).~~ **Resolved (reversed 2026-08-02, reversed again 2026-08-03):** auto-aim-to-nearest-enemy shipped and was briefly the canonical scheme, but a real bullet locking onto a target read wrong while the player was still trying to manually aim elsewhere, so it was pulled — manual aim (mouse point / stick dir) became the ONLY aim input for a while, exactly like an enemy's own `facing` (`AIDecideSystem`). **v33 removes manual aim too**, rather than restoring the toggle: there is no aim input at all now — the engine auto-faces the nearest hostile, else the movement direction, else holds last facing (`ApplyInputSystem`). This avoids the earlier failure mode outright, since there's no manual input left for an auto-lock to fight. (The touch stick/button hit-zones this note used to flag as invisible were given a real render layer earlier, 2026-07-29 — `client/src/game/ui/TouchControlsView.ts` draws `platform/TouchControls.ts`'s geometry, latched on the first touch so a mouse player never sees it. What is still unverified is how any of it feels under real thumbs on a real device — `04`'s checklist item 5, blocked on hardware, not on code.)
- ~~**Pause semantics in co-op:** is there any local "menu" that must not stall the frame stream, or is everything a non-blocking overlay (`06`)?~~ **Resolved, see Screen flow above:** online is a documented no-op for now (`PauseMenu.ts` isn't built for that path) — everything online stays a non-blocking overlay, matching "no true pause" (`06`).
- ~~**Button count vs. clutter:** four corner buttons + two sticks is already busy in landscape; does weapon-swap need two dedicated slots or one toggle? Affects `05`'s control scheme and the `buttons` bitfield (`08`).~~ **Resolved:** favor a few large, clearly-labeled clickable elements over dense text/many small controls — the Main Menu (2 buttons), the Loadout screen (row buttons + one primary Start action), and the result screen (one primary confirm + one secondary link) all follow this. Weapon-swap keeps **two** dedicated slot buttons on screen while the `buttons` bitfield keeps **one** toggle — the answer turned out to be both, bridged in the client (`weaponSlotSelect.ts`). The bitfield is unchanged, as this line always claimed; what changed on 2026-09-03 is that the two buttons stopped behaving as one.
- ~~**HUD for spectators / downed co-op players** (revive UI) — tied to `05`'s open death/penalty question.~~ **Resolved (2026-08-03):** `ui/DownedBanner.ts` gives the LOCAL seat's own downed state a real banner (bleedout countdown, then a revive-channel progress readout) — previously only the teammate's own `AllyRow` showed anything, and a downed local player saw nothing explaining why the world had gone quiet. `AllyRow` itself gained the matching "REVIVING {pct}%" branch. No dedicated spectator mode exists (none of this repo's modes have one) — this closes the "downed player" half only.
